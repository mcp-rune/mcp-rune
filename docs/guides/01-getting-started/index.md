# Getting Started

Your first day with mcp-rune — install, scaffold, and run a server end to end.

**Read in this order:**

1. [Quickstart](./quickstart.md) — scaffold a project, register your first model, and call a tool from Claude Desktop
2. [Project structure](./project-structure.md) — where models, prompts, tools, apps, and the domain registry live

When you're ready to point models at a real backend, read [API configuration](../06-the-three-layers-up-close/api-configuration.md) in chapter VI — the static `api` block sits with `ApiClient`, `ApiConvention`, and `ModelService`, the three collaborators that consume it.
