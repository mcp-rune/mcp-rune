# Apps, Search & Forms

Rich UI surfaces — sandboxed HTML apps, schema-derived search filters, and per-field-group form customization.

**Read in this order:**

1. [MCP apps](./mcp-apps.md) — schema-driven HTML forms rendered inside the client; `ontoolinput`, `ontoolresult`, `callServerTool`
2. [Apps architecture](./mcp-apps-arch.md) — how the framework resolves a `ui://` resource, sandboxes the iframe, wires message channels
3. [Search filters](./search-filters.md) — the shared filter contract: text · enum · relation · date_range · integer_range
4. [Model form customization](./model-form.md) — horizontal grids, row layouts, stacked variants, per-fieldGroup overrides
