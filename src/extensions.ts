// mcp-rune/extensions — types for authoring HTTP extensions.
// Built-in extensions live under `@mcp-rune/mcp-rune/extensions/<name>`
// (e.g. `./extensions/cimd`). See docs/guides/extensions.md.
export type {
  HttpExtension,
  HttpExtensionCapability,
  HttpExtensionContext,
  HttpExtensionMap
} from './mcp/extensions/types.js'
